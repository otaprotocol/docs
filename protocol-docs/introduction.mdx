---
title: 'Introduction'
description: 'Example section for showcasing API endpoints'
---

The Action Codes Protocol provides a unified, chain-agnostic interface for creating, validating, and finalizing intent-bound one-time action codes. Below is a complete reference of the core methods exposed by the protocol.

###

To use Action Codes Protocol simply install the package.
```bash
npm install @actioncodes/protocol
```
